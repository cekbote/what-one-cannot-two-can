# What One Cannot, Two Can

1. Install all required libraries from the `requirements.txt` file:

   ```bash
   pip install -r requirements.txt
   ```

2.	Run the code by executing the main script:

    ```bash
    python src/main.py
    ```